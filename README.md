# CraftLaunch

A Minecraft third-party launcher built with Electron + React + Tailwind CSS.

## Development

```bash
npm install
npm run dev          # Vite dev server (React)
npm run build:electron  # Build Electron + React
npm run dev:electron    # Run Electron app
```

## Build

```bash
npm run build:electron
